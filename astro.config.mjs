// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Sustituye por tu dominio real (Vercel, Netlify, o dominio propio)
  site: 'https://www.naun.xyz', 
});